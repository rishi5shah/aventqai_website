# Copy to .env.local and fill in. See README.md "CRM integration (GoHighLevel)".

# URL of a GoHighLevel Workflow with an "Inbound Webhook" trigger.
# Leave unset in development — leads still log to the console and the route
# still returns ok:true; the GHL forward is just skipped.
GHL_WEBHOOK_URL=
